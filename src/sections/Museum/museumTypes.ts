/** The museum's three moments: admiring the doors, doors swinging, wandering. */
export type MuseumPhase = 'entrance' | 'opening' | 'inside'

/** Measured world size of a hung photo (set once its texture loads). */
export type PhotoSize = { width: number; height: number }
